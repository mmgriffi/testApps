name: Octo Organization CI

on:
 workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Run a one-line script
      - id: step1
        run: echo Hello from Octo Organization
             echo Hope we can run another
            

